# Pi Extensions

This project currently has no browser automation integration installed. A replacement tool can be built cleanly later.

Current extension inventory is defined by `.pi/settings.json` and `.pi/extensions/`. Use `pi list` plus `.pi/smoke-test.sh` to verify installed packages and active tools.
