# Direct Python dependencies for the root local Pi/room-audio runtime.
# Transitive dependencies are resolved by pip; this is not a pip-freeze lock.

beautifulsoup4==4.14.3
huggingface_hub==1.14.0
piper-tts==1.4.2
requests==2.33.1
