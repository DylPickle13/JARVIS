# Blank private hub/cloud settings template. Copy to .env and chmod 600 locally.
# Never paste credentials into chat, CLI arguments, stream URLs or Git.
JARVIS_SECURITY_HUB_HOST=
JARVIS_SECURITY_USERNAME=
JARVIS_SECURITY_PASSWORD=
