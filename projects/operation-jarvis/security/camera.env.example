# Blank template only. Not automatically loaded by any security command.
# Keep actual values in a private secret manager/environment, not chat or logs.
# Never put a username/password into a stream URL or a shell command argument.
# Camera Account credentials are separate from your Tapo/TP-Link cloud account.
JARVIS_SECURITY_CAMERA_HOST=
JARVIS_SECURITY_CAMERA_USERNAME=
JARVIS_SECURITY_CAMERA_PASSWORD=
