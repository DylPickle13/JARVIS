{
  "name": "operation-jarvis-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "LAN HUD and safe control surface for Operation JARVIS.",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "HOST=0.0.0.0 PORT=8787 node --watch src/server.mjs",
    "check": "node --check src/server.mjs && node --check public/display.js && node --check public/dashboard-voice.js",
    "url": "node scripts/lan-url.mjs",
    "install-service": "bash scripts/install-launch-agent.sh",
    "uninstall-service": "bash scripts/uninstall-launch-agent.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "onnxruntime-web": "^1.23.2"
  }
}
